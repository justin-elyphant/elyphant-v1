import React from "react";
import ThemeSettings from "./ThemeSettings";

const GeneralSettings = () => {
  return (
    <div className="space-y-6">
      <ThemeSettings />
      {/* Keep other general settings here */}
    </div>
  );
};

export default GeneralSettings;
