
import React from "react";

// This component is no longer used as we're using the main Header component 
// from the homepage for consistency
const MarketplaceHeader = () => {
  return null;
};

export default MarketplaceHeader;
