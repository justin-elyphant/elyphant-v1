
import React from "react";
import Hero from "../Hero";

const HeroSection: React.FC = () => {
  return (
    <section>
      {/* Hero Component with integrated countdown */}
      <Hero />
    </section>
  );
};

export default HeroSection;
